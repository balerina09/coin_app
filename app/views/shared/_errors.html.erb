<% if object.errors.any? %>
    <div style="color: red">
      <h3><%= pluralize(object.errors.count, "error") %> prohibited this category from being saved:</h3>

      <ul>
        <% object.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
<% end %>
